# Legal RAG — зависимости (локальный стек: LangChain + Ollama)

# Загрузка с Adilet (кодексы всегда обновляются)
requests>=2.28.0
beautifulsoup4>=4.12.0

# LangChain (langchain — для EnsembleRetriever)
langchain>=0.3.0
langchain-classic>=1.0.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-pinecone>=0.1.0
pinecone>=5.0.0
langchain-huggingface>=0.1.0
langchain-ollama>=0.2.0
langchain-text-splitters>=0.3.0

# .env
python-dotenv>=1.0.0



# Векторная БД и эмбеддинги (Pinecone — облако)
sentence-transformers>=2.2.0

# Гибридный поиск: BM25
rank_bm25>=0.2.2

# Reranker для топ-релевантных чанков
flashrank>=0.2.0
FlagEmbedding>=1.2.0
peft>=0.10.0

# Облачный LLM (Groq) — опционально, включается LEGAL_RAG_LLM_BACKEND=groq
langchain-groq>=0.1.0

# UI
streamlit>=1.28.0

# Бенчмарк и метрики
pandas>=2.0.0
openpyxl>=3.1.0
rouge-score>=0.1.2
nltk>=3.8.0

# Опционально: Graph RAG (Neo4j)
# neo4j>=5.0.0

# Опционально: Jina embeddings (SOTA для казахского, нужен API key)
# jina>=0.2.0
# API
fastapi>=0.100.0
uvicorn>=0.20.0
